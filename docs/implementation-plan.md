# Implementation plan

1. Establish a verified PyCharm/Gradle/Kotlin baseline.
2. Build persistent selection, deterministic analysis and batch-aware context rendering.
3. Add safe staging, real-file drag/clipboard and history.
4. Add strict patch parsing, PSI preview and undoable replace/add operations.
5. Finish UI, settings, prompt skills, tests, documentation, CI and install scripts.
6. Require successful build/tests and a generated plugin ZIP before release.
