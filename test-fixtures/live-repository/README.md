# Live fixture repository

This deterministic repository is used only for the full PyCharm UI acceptance test of Copilot Context Bridge.

## Development

- Production Python lives under `src/`.
- Tests mirror production modules under `tests/`.
- Configuration lives under `config/`.
- Run tests with `pytest`.

